<!DOCTYPE html>
<html>
<head>
  <title>AuthorLink</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= javascript_include_tag "jquery" %>
</head>

<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand">RubyTrivia</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        </ul>

        <!-- Search Form -->
        <%= form_with url: users_backoffice_search_path, local: true, class: "d-flex navbar-form ms-auto", method: :get do |form| %>
          <div class="form-group me-2">
            <%= form.text_field :term, class: "form-control", placeholder: "type to search..." %>
          </div>
          <button type="submit" class="btn btn-outline-success my-2 my-sm-0">Search</button>
        <% end %>

        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= button_to destroy_user_session_path, method: :delete, class: "nav-link" do %>
              <%=t('Sair')%>
              <span class="bi bi-box-arrow-right fs-6"></span>
            <% end %>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container-fluid">
    <div class="row flex-nowrap">
      <!-- Sidebar -->
      <div class="col-md-2 bg-dark text-light vh-100 sidebar">
        <div class="profile text-center my-4">
          <img src="<%= image_url('profile.jpg') %>" alt="Profile Picture" class="rounded-circle" width="100">
          <h4 class="mt-2">Nome do Usuário</h4>
        </div>
        <div class="nav flex-column">
          <%= link_to "Home", users_backoffice_home_index_path, class: "nav-link text-light" %>
          <%= link_to "Books", '#', class: "nav-link text-light" %>
          <%= link_to "Authors", '#', class: "nav-link text-light" %>
          <!-- Adicione outros links conforme necessário -->
        </div>
      </div>
      
      <!-- Main Content -->
      <div class="col-md-10 main-content">
        <div class="container mt-4">
          <%= yield %>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
